---
phase: 02-project-member-entry
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - static/js/grid-manager.js
  - static/js/batch-edit.js
  - static/js/app.js
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03, CALC-04, CALC-05, ENTRY-08, ENTRY-09]
---

<objective>
Wire AG Grid cell edits to the DFT calculation pipeline and implement batch editing for multi-row operations.

Purpose: Every edit triggers an instant recalculation. Users see DFT, area, and litres update live as they type. Batch edit lets them change fire rating or product across multiple selected members at once.
Output: Live calculation display in AG Grid with no "calculate" button, plus batch edit capability.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Live DFT calculation on cell edit and batch edit</name>
  <files>
    static/js/grid-manager.js
    static/js/batch-edit.js
    static/js/app.js
  </files>
  <action>
**Update static/js/grid-manager.js** — Add calculation wiring:
- `onCellValueChanged(event)` handler (already stubbed in Plan 03):
  - When section_id, hp_profile_name, fire_rating_id, failure_temp_id, product_id, quantity, or length_m changes:
    call api.updateMember(projectId, memberId, changedFields)
  - API response contains recalculated hp_over_a, dft_mm, final_dft_mm, surface_area_m2, volume_litres, weight_kg, status
  - Update the row data with response values using gridApi.applyTransaction({update: [rowData]})
  - Flash changed cells with cellFlashDelay for visual feedback (CALC-05 — live updates)
- Column value formatters:
  - DFT: show as mm with 3 decimal places, and also show microns in tooltip (e.g., "0.367 mm (367 µm)")
  - Area: 2 decimal places + "m²" suffix
  - Litres: 2 decimal places + "L" suffix
  - Hp/A: integer + "m⁻¹" suffix
- Status column cellRenderer: colored dot (green/amber/red/grey) matching status field

**static/js/batch-edit.js** — Batch edit module:
- `showBatchEditDialog(selectedRows, field)` — shows a small modal/popover
  - Fields that can be batch-edited: fire_rating_id, failure_temp_id, product_id, zone, level
  - For fire_rating_id: show dropdown of available fire ratings from current product
  - For failure_temp_id: show dropdown of available temps
  - For product_id: show dropdown of all products
  - For zone/level: show text input
- On confirm: call api.updateMember for each selected row with the new value
  - Collect all responses, update grid rows with recalculated values
  - Show progress ("Updating 5 of 12 members...")
- Trigger: right-click context menu on selected rows OR toolbar button
- Also support: select all (Ctrl+A), then batch edit

**Update static/js/app.js** — Wire batch edit:
- Add batch edit toolbar button (shown when multiple rows selected)
- Add right-click context menu with batch edit options
- Add project summary bar at bottom: total members, total litres, total kg (read from summing grid data)
- Summary updates on every grid data change
  </action>
  <verify>
    <automated>cd "C:/Users/Little Nineveh/Nullifire Intumescent Calculator" && python -c "
import os
# Verify batch-edit.js exists and has key patterns
path = 'static/js/batch-edit.js'
assert os.path.exists(path), f'{path} missing'
content = open(path).read()
assert 'batchEdit' in content or 'BatchEdit' in content or 'batch' in content.lower()
print(f'{path}: OK ({len(content)} chars)')

# Verify grid-manager has calculation wiring
gm = open('static/js/grid-manager.js').read()
assert 'updateMember' in gm or 'applyTransaction' in gm
assert 'valueFormatter' in gm or 'cellRenderer' in gm
print('grid-manager.js has calculation wiring')
print('PASS')
"</automated>
  </verify>
  <done>Cell edits trigger immediate DFT recalculation via API, computed columns update with formatted values, batch edit modal works for multi-row property changes, summary bar shows project totals</done>
</task>

</tasks>

<success_criteria>
- Every cell edit that affects DFT triggers recalculation — no manual "calculate" button (CALC-05)
- Hp/A auto-calculated from section + exposure (CALC-01)
- DFT looked up from certification data (CALC-02)
- Surface area = heated_perimeter * length * qty (CALC-03)
- Volume = area * DFT / solid_factor (CALC-04)
- Inline edit any cell with live recalc (ENTRY-08)
- Multi-row batch edit for fire rating, product, temp (ENTRY-09)
- Project summary bar shows running totals
</success_criteria>
