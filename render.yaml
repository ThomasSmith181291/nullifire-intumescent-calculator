services:
  - type: web
    name: nullifire-calculator
    runtime: python
    buildCommand: pip install -r requirements-web.txt
    startCommand: gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 2
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
